<svg fill="none" viewBox="0 0 25 25" width="25" height="25" xmlns="http://www.w3.org/2000/svg">
    <foreignObject width="100%" height="100%">
        <div xmlns="http://www.w3.org/1999/xhtml">
            <style>
.orig {
  position: relative;
  margin: 0 auto;
  animation: rot_orig 1.5s infinite ease-in-out;
  height: 50px;
  width: 0;
}

.dot {
	position: absolute;
  border-radius: 50%;
	font-size: 25px;
}

.one {
  animation-delay: 0s;
  border-radius: 50%;
  color: #000;
}

.two {
  animation-delay: 100ms;
  border-radius: 50%;
  color: #000;
}

.three {
  animation-delay: 200ms;
  border-radius: 50%;
  color: #000;
}

.four {
  animation-delay: 300ms;
  border-radius: 50%;
  color: #000;
}

              @-webkit-keyframes rot_orig {
	from {
		transform: rotate(0deg) translate(0, -.5em) rotate(0deg);
	}
	to {
		transform: rotate(360deg) translate(0, -.5em) rotate(-360deg);
	}
}
            </style>
            <div class='orig'>
                        <div class='dot orig one'>.</div>
                        <div class='dot orig two'>.</div>
                        <div class='dot orig three'>.</div>
                        <div class='dot orig four'>.</div>
            </div>
        </div>
    </foreignObject>
</svg>
